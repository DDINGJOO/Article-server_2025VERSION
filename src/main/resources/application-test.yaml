server:
  port: 8000
spring:
  datasource:
    url: jdbc:mariadb://localhost:4000/articles?useSSL=false&serverTimezone=seoul
    username: root
    password: pass123#
    driver-class-name: org.mariadb.jdbc.Driver


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect




  sql:
    init:
      mode: never





